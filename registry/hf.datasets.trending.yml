# Field shape verified against the live API on 2026-08-31 (see git history).
source_id: hf.datasets.trending
status: active
cadence: daily
schema_id: adoption.v1

publisher: Hugging Face
publisher_tier: first_party
destroys_own_history: true   # rolling windows and running totals only; no history endpoint
licence: "Hub metadata via public API; see https://huggingface.co/terms-of-service"
personal_data: none
storage: git

notes: >-
  Interest 3: the leading indicator for what kinds of datasets are wanted right now.

endpoints:
  - url: "https://huggingface.co/api/datasets?sort=trendingScore&direction=-1&limit=1000&expand[]=trendingScore&expand[]=downloads&expand[]=likes&expand[]=createdAt"
    delay_seconds: 2

gates:
  expect_status: 200
  min_bytes: 10000
  content_type_any: [json]
  must_contain: ["downloads"]
  must_not_contain: ["Access Denied"]
  max_shrink_pct: 50
